# PulseBoard C++ Demo

This project is the PulseBoard score service. It computes the daily score and status from habit completion data.

## Build

```bash
cmake -S . -B build
cmake --build build
```

## Run

```bash
./build/pulseboard
```
