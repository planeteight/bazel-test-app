# PulseBoard C++ Demo

This project is the PulseBoard momentum service. It analyzes a week of score snapshots and emits trend, momentum, volatility, and risk action outputs.

## Build

```bash
cmake -S . -B build
cmake --build build
```

## Run

```bash
./build/pulseboard
```
